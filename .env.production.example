# Production Environment Configuration
# Copy this file to .env and update with your production values

# ============================================
# Server Configuration
# ============================================
NODE_ENV=production
PORT=8085
HOST=0.0.0.0

# ============================================
# Database Configuration (SQL Server)
# ============================================
DB_SERVER=your_production_db_server_ip
DB_NAME=MeterOCRDESCO
DB_USER=your_production_db_user
DB_PASSWORD=your_secure_production_password
DB_PORT=1433

# ============================================
# JWT Configuration
# ============================================
# IMPORTANT: Generate a strong random secret for production
# You can generate one using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CHANGE_THIS_TO_A_STRONG_RANDOM_SECRET_IN_PRODUCTION
JWT_EXPIRE=24h
JWT_REFRESH_EXPIRE=7d

# ============================================
# Upload Configuration
# ============================================
UPLOAD_PATH=./uploads
MAX_FILE_SIZE=10485760

# ============================================
# CORS Configuration
# ============================================
# IMPORTANT: Set this to your frontend domain in production
# Examples:
# - Single domain: https://yourdomain.com
# - Multiple domains (comma-separated): https://yourdomain.com,https://app.yourdomain.com
# - Development (allow all): *
CORS_ORIGIN=*

# ============================================
# API Configuration
# ============================================
API_VERSION=v1
API_PREFIX=/api

# ============================================
# Production Security Checklist
# ============================================
# [ ] Changed JWT_SECRET to a strong random value (minimum 64 characters)
# [ ] Updated DB_PASSWORD to a strong password
# [ ] Set CORS_ORIGIN to specific domain(s)
# [ ] Verified NODE_ENV=production
# [ ] SSL/HTTPS configured in IIS or load balancer
# [ ] Firewall rules configured for port 8085
# [ ] Regular database backups scheduled
# [ ] Monitoring and logging configured
# [ ] .env file permissions restricted (not readable by other users)
